import {Entity, PrimaryGeneratedColumn, Column} from "typeorm";

@Entity()
export class Tasks {

    // add the following columns (w/ the proper decorators):

    // id - this is the primary key (the value should be auto generated)

    // title

    // description

    // finished - the default should be false

    // created_at

    // updated_at

}
